import styled from "styled-components";

export const StyledRegalo = styled.li`
    margin-top: 10px;
    list-style:none;
    position:relative;
    p{
        font-weight: 600;
    }
    span{
        font-weight: 500;
    }
    button{
        /* position: absolute; */
        top: 0;
        right: 0;
        padding: 5px 8px;
        border-radius: 10px;
        border: none;
        background-color: red;
    }
`